import { SwaggerConfig } from './swagger.interface';

// Configuration for the swagger UI
export const SWAGGER_CONFIG: SwaggerConfig = {
  title: 'Statement API',
  description: 'Endpoints should used by Statement client.',
  version: '1.0.0',
  tags: [],
};
